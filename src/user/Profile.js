import React from "react";
import Base from "../core/Base";

const Profile = () => {
  return (
    <Base title="This is the Profile Page">
      <h1>This is the profile page</h1>
    </Base>
  );
};

export default Profile;
