import React from "react";
import Base from "../core/Base";

const UserDashboard = () => {
  return (
    <Base title="This is the UserDashboard Page">
      <h1>This is the UserDashboard page</h1>
    </Base>
  );
};

export default UserDashboard;
